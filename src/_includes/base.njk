<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{% if tag %}Tagged "{{ tag }}" {% endif %}{{ title }}</title>
    <meta name="description" content="{{ excerpt | safe }}" />
    <link rel="canonical" href="{{ page.url | url | safe }}" />
    <meta name="robots" content="index, follow" />
    <meta property="og:title" content="{% if tag %}Tagged "{{ tag }}" {% endif %}{{ title }}" />
    <meta property="og:url" content="{{ page.url | url | safe }}" />
    <meta property="og:description" content="{{ excerpt | safe }}" />
    <meta property="og:image" content="{{ image | safe }}" />
    <meta property="og:type" content="{{ type | safe}}" />

    <link rel="icon" href="/images/favicon.ico" type="image/x-icon" />

    <link rel="stylesheet" href="{{'/css/styles.css' | url | safe}}" />

    <script src="{{'/js/scripts.js' | url | safe}}"></script>

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-LEWK3NM245"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-LEWK3NM245');
    </script>


  </head>
  <body>
    <header>
      <nav>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/writing"> Writing</a></li>
          <li><a href="/projects">Projects</li></a></li>
           </ul>
      </nav>
        
    </header>

    <main>{{ content | safe }}</main>

    <footer>
      <p>&copy; Anna Cunnane 2026</p>
    </footer>
  </body>
</html>
